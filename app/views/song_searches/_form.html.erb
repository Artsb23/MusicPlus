<%= form_for(@song_search) do |f| %>
  <% if @song_search.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song_search.errors.count, "error") %> prohibited this song_search from being saved:</h2>

      <ul>
      <% @song_search.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :artist_name %><br>
    <%= f.text_field :artist_name %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :spotify_id %><br>
    <%= f.text_field :spotify_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
